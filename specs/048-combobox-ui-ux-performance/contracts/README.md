# Component API Contracts

As a purely internal frontend UI/UX refactoring task, no external REST API contracts or command-line interface schemas are exposed. 

The internal React component props are defined in [data-model.md](../data-model.md).
