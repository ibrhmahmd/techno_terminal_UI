# Implementation Plan: Groups Filtering — Audit Fix

**Branch**: `044-groups-filtering-audit` | **Date**: 2026-06-10 | **Spec**: specs/044-groups-filtering-audit/spec.md
**Input**: Feature specification from `/specs/044-groups-filtering-audit/spec.md`

**Note**: This template is filled in by the `/speckit.plan` command. See `.specify/templates/plan-template.md` for the execution workflow.

## Summary

Fix 29 audit findings across the groups filtering feature: 5 runtime bugs (stale activeCategoryKey, totalGroups in grouped view, status label inconsistency, filter count mismatch, keyboard guard), 5 dead code removals (4 API functions + 1 unused export), 6 TypeScript violations (unsafe type assertions, missing return types, redundant casts, unmemoized object), 10 accessibility gaps (Pagination ARIA, radio group roles, aria-live regions, ErrorBoundary isolation, responsive layout), and 2 data fetching anti-patterns (manual fetch migration, staleTime alignment). All frontend-only — no backend changes.

## Technical Context

**Language/Version**: TypeScript ~5.9
**Framework**: React 19 + Vite 8
**Primary Dependencies**: React Router DOM 7, TanStack React Query 5, Zustand 5, Axios 1, Tailwind CSS 3.4, Lucide React 1, Recharts 3
**Styling**: Tailwind CSS v3.4 (v3 config, despite `@tailwindcss/postcss` v4 in package.json)
**Testing**: Vitest 4.1 + happy-dom (not jsdom) — test files in `src/tests/`, setup in `src/test/setup.ts`
**Target Platform**: Browser (modern Chrome, Firefox, Safari, Edge)
**Project Type**: Frontend SPA (React single-page application)
**API**: Axios client at `src/api/client.ts`, base URL `/api/v1`, JWT Bearer auth with auto-refresh
**Icons**: Material Symbols (`material-symbols-outlined` CSS class) + Lucide React components
**Fonts**: Space Grotesk (`font-headline`) for headings, Inter (`font-body`) for body text
**Performance Goals**: <1s initial load, <200ms navigation, 60fps animations
**Constraints**: Frontend-only — no backend code. Strict TS (`noUnusedLocals`, `noUnusedParameters`, `verbatimModuleSyntax`). Build must pass `tsc -b && vite build`.
**Scale/Scope**: Single-page CRM with 18 pages, ~13 API domain modules

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Frontend-Only Scope | ✅ PASS | All changes in `src/`. No backend code. |
| II. Server State Discipline | ✅ PASS | US5 migrates manual fetch to React Query; aligns staleTime to convention. |
| III. Global State Minimalism | ✅ PASS | No new Zustand stores. |
| IV. TypeScript Strict Mode | ✅ PASS | US3 removes `as` casts, tightens type safety. |
| V. Component Naming Convention | ✅ PASS | No new components — only modifications to existing files. |
| VI. Cache & API Discipline | ✅ PASS | All changes align with queryKeys factory pattern. |

**All gates pass.** No complexity violations to track.

## Project Structure

### Documentation (this feature)

```text
specs/044-groups-filtering-audit/
├── plan.md              # This file
├── research.md          # (skipped — no unknowns)
├── data-model.md        # (skipped — no new entities)
├── contracts/           # (skipped — no new interfaces)
├── quickstart.md        # (skipped — no new user-facing features)
└── tasks.md             # Generated by /speckit.tasks
```

### Source Code Changes

```text
src/
├── api/academics/
│   ├── groups/
│   │   ├── core.ts                  # ← US2: remove 4 dead functions + deduplicate paramsSerializer
│   │   └── index.ts                 # ← US2: remove dead exports from barrel
│   └── index.ts                     # ← US2: remove dead re-exports from parent barrel
├── components/
│   ├── common/
│   │   ├── Pagination.tsx            # ← US4: aria-labels, aria-current, aria-hidden
│   │   └── datatable/RowActions.tsx  # ← US4: aria-hidden on icons
│   ├── groups/
│   │   ├── GroupFilters.tsx          # ← US1: fix filter count badge; US4: role="region", aria-live
│   │   ├── GroupBySelector.tsx       # ← US1: keyboard guard; US4: radiogroup role
│   │   └── GroupColumns.tsx          # ← US1: fix unknown status label
│   └── enrollments/
│       └── DropEnrollmentPanel.tsx   # ← US5: migrate to useGroupsFlat
├── hooks/
│   ├── useGroups.ts                  # ← US1: fix totalGroups in grouped view; US3: type guard, memoize filters, remove redundant cast
│   └── useGroupQueries.ts            # ← US2: drop groupKeys export; US5: fix staleTime
└── pages/
    └── GroupsPage.tsx                # ← US1: reset activeCategoryKey on groupBy change; US4: ErrorBoundary split, aria-live, responsive toolbar
```
