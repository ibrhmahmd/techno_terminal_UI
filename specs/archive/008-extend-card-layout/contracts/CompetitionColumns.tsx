// CompetitionColumns — component contract
// Column definitions for rendering competitions in a DataTable
// Follows the same pattern as GroupColumns.tsx
// Columns: name, location, competition_date, edition, fee_per_student, deleted status indicator
// Reuses: DataTableColumn<Competition> type from src/components/common/datatable/types.ts
